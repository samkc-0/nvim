# Setup
## macOS

① install **neovim** in terminal: `brew install neovim`.

② cd to `~/.config` and run `git clone https://github.com/samkc-0/nvim.git`.

③ open `~/.config/nvim/init.lua` with **neovim** and run `:so %`.


